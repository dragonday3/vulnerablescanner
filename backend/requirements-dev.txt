# Dev/CI-only tooling: test runner, linters, formatter, type checker. Not
# required to run the app itself — see requirements.txt for runtime
# dependencies (httpx included — it moved there in Phase 3, since the HTTP
# fingerprinting module now uses it at runtime, not just in tests).
#
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# The Docker image installs this file too (see backend/Dockerfile) so that
# `docker compose exec backend pytest -v` / `ruff` / `black` / `mypy` keep
# working exactly as before — the split here is about declaring runtime vs.
# dev intent clearly in source control, not about physically excluding dev
# tools from the running container.
pytest~=9.1
pytest-cov~=7.1
ruff~=0.16
black~=26.5
mypy~=2.3
